# 🐛 TROUBLESHOOTING GUIDE

## Проблем: Бутонът "Generate Script" не работи

### ✅ РЕШЕНИЕ - Стъпка по стъпка:

#### 1️⃣ Отвори Файла
- Отвори `payment-script-generator-pro.html` в браузъра
- Натисни `F12` за да отвориш **Developer Console**

#### 2️⃣ Провери DEBUG Информацията
- В горния десен ъгъл има нов **🐛 DEBUG** бутон (оранжев)
- **Натисни го** и виж какво показва:
  ```
  Current Type: [трябва да е: cart/payment/disable/autofill/total]
  Current Step: [трябва да е: 2]
  Selected Element: [трябва да има selector]
  Candidates: [трябва да е > 0]
  Generate Button: [трябва да е: ENABLED]
  ```

#### 3️⃣ Проследи Процеса в Конзолата

Когато натиснеш **"Generate Script"**, в конзолата трябва да видиш:

```
=== GENERATE SCRIPT CALLED ===
selectedElement: {selector: "...", score: ...}
currentType: "total"  (или cart/payment/etc.)
Config found: {...}
Confidence calculated: 85
Scripts generated: {...}
Populating script variants...
Generating documentation...
Moving to step 3...
=== SCRIPT GENERATION COMPLETE ===
```

### 🔍 Какво може да е проблема:

#### **Проблем A: "currentType" е NOT SET**
**Причина:** Не е избран тип скрипт в Step 1
**Решение:** 
1. Върни се на Step 1
2. Кликни на един от 5-те бутона (Cart Items, Payment Methods, и т.н.)
3. Провери че бутонът стана син (selected)
4. После отиди на Step 2

#### **Проблем B: "selectedElement" е NOT SELECTED**
**Причина:** Не е избран кандидат в Step 2
**Решение:**
1. След Analyze Result, виждаш списък с кандидати
2. **КЛИКНИ** на един от тях (картата трябва да стане синя)
3. Бутонът "Generate Script" трябва да стане тъмно син
4. Сега кликни на него

#### **Проблем C: Бутонът е DISABLED (сив)**
**Причина:** Не е избран кандидат
**Решение:** Виж **Проблем B**

#### **Проблем D: JavaScript грешка в конзолата**
**Причина:** Възможен конфликт или грешка в кода
**Решение:**
1. Refresh страницата (F5)
2. Започни отново от Step 1
3. Ако пак не работи, изпрати screenshot на конзолата

### 📋 Правилен Workflow:

```
Step 1: Select Script Type
  ↓
  [Избери типа → бутонът става син]
  ↓
  Click "Next" → Step 2

Step 2: Run Diagnostic & Select Element
  ↓
  [Paste diagnostic JSON]
  ↓
  Click "Analyze Result"
  ↓
  [Виждаш кандидатите]
  ↓
  CLICK на кандидат → става син
  ↓
  Бутонът "Generate Script" става активен (тъмно син)
  ↓
  Click "Generate Script" → Step 3

Step 3: View Generated Scripts
  ↓
  [Виждаш 3 варианта на скрипта]
```

### 🛠️ Advanced Debugging:

Ако горните стъпки не помагат, в конзолата напиши:

```javascript
// Провери глобалните променливи
console.log('currentType:', currentType);
console.log('selectedElement:', selectedElement);
console.log('candidates:', candidates);

// Опитай ръчно да извикаш функцията
generateScript();
```

Ако видиш някоя грешка, копирай я и ми я изпрати!

### 🎯 Бърз Fix:

Ако нищо не работи:
1. Затвори браузъра напълно
2. Изтрий cache (Ctrl+Shift+Del → Clear browsing data)
3. Отвори файла отново
4. Пробвай пак

---

## 📞 Нужда от помощ?

Ако проблемът продължава:
1. Натисни **🐛 DEBUG** бутона
2. Screenshot на popup-а
3. Отвори Developer Console (F12)
4. Screenshot на Console tab-а
5. Изпрати ми двата screenshots

Ще го поправя веднага! 💪
